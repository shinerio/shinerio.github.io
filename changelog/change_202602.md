## macOS风格代码块样式与全屏查看功能
date: 2026-02-19
- 重新设计代码块顶部为 macOS 窗口风格的 header bar，左侧显示红黄绿三色圆点装饰
- 语言标签居中显示在 header bar 中，复制按钮和新增的全屏按钮移至右侧
- 新增全屏查看功能：点击展开图标可在全屏遮罩层中查看完整代码，支持点击关闭按钮、点击背景、按 Escape 键三种方式关闭
- 深色/浅色主题均适配，移动端按钮默认可见

## 支持Mermaid图表渲染
date: 2026-02-21
- 在 `SiteGenerator.ts` 中添加 marked 自定义渲染器，将 `mermaid` 语言的代码块输出为 `<pre class="mermaid">` 而非普通 `<pre><code>` 结构
- 文章页引入 mermaid.js v11 ESM 模块（CDN），根据当前主题自动切换 dark/default 渲染风格
- 在 `style-addition.css` 中添加 mermaid 专用样式：重置代码块的 padding/border/背景、隐藏 code-enhance header、SVG 自适应宽度
- 已通过 OAuth2.0 文章中的序列图验证渲染效果

## 修复首页彩虹字体和流星雨效果
date: 2026-02-19
- 在 `src/core/SiteGenerator.ts` 的首页 hero 区块硬编码 HTML 中，添加了缺失的 `.meteor-shower` 流星雨 div 结构（8个 `<span class="meteor">` 元素）
- 将描述文字的 CSS 类名从 `hero-subtitle` 修正为 `hero-description`，使其与 CSS 中定义的彩虹渐变动画 `.hero .hero-description` 正确匹配

## 修复流星雨动画视觉效果
date: 2026-02-22
- 为 `.hero` 区域添加天空渐变背景：浅色模式使用暖色调桃色渐变，深色模式使用深蓝夜空渐变
- 通过 `.meteor-shower::before` 伪元素添加约20颗星点装饰，浅色模式隐藏、深色模式可见
- 修复流星运动方向：将 CSS transform 中 `translateY` 和 `rotate(35deg)` 的顺序互换，使流星沿旋转后的轴线对角移动，与条纹角度一致
- 添加 `prefers-color-scheme: dark` 媒体查询，确保 auto 主题用户也能正确看到深色模式下的天空背景、星点和蓝色流星
