# 1. 腾讯
- CCN路由同步，默认会同步所有路由表
- [CCN一个VPC可以接入多个CCN](https://cloud.tencent.com/document/product/877/96123)
- VPC多条路由冲突的时候，可以支持启用、停用
## 1.1. 跨地域流量管理
- 单向的流量调度规则限速带宽总和不得超过带宽上限。
- 带宽上限提高后，默认规则的限速带宽不会自动调整，需手动调整

# 2. 阿里
- CEN路由同步，默认只会同步默认路由表，用户可以选择默认路由表或所有路由表

# 3. aws
- 一个vpc只能挂一个attachment